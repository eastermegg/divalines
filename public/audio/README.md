# Audio

The vinyl widget now plays a **Spotify playlist embed** (see
`SPOTIFY_PLAYLIST_ID` in `lib/site.ts`) instead of a local loop — no
audio files are needed here. Directory kept in case a local ambience
loop returns.
